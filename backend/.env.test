# Test Environment Configuration
NODE_ENV=test
PORT=3334

# Test Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kxrtex_test_db
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kxrtex_test_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false

# Test Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Test JWT
JWT_SECRET=test_jwt_secret_key_for_testing_only_do_not_use_in_production
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=test_refresh_secret_key_for_testing_only
JWT_REFRESH_EXPIRES_IN=7d

# Disable external services in tests
CLOUDINARY_CLOUD_NAME=test_cloud
CLOUDINARY_API_KEY=test_key
CLOUDINARY_API_SECRET=test_secret

# Disable ASAAS in tests
ASAAS_API_KEY=test_asaas_key
ASAAS_API_URL=http://localhost:3000/mock-asaas
ASAAS_WEBHOOK_TOKEN=test_webhook_token

# Disable email in tests
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=test@kxrtex.com

# Disable push notifications in tests
VAPID_PUBLIC_KEY=test_vapid_public
VAPID_PRIVATE_KEY=test_vapid_private
VAPID_SUBJECT=test@kxrtex.com

# Test specific settings
TEST_VERBOSE=false
LOG_LEVEL=error